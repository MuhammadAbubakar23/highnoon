import { SelectionFiltePipe } from './selection-filte.pipe';

describe('SelectionFiltePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectionFiltePipe();
    expect(pipe).toBeTruthy();
  });
});
